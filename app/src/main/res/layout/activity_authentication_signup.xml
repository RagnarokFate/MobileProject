<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- ScrollView to allow scrolling if the content exceeds the screen size -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Main content container using CardView for a cleaner look -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="8dp">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp">

                <!-- Title -->
                <TextView
                    android:id="@+id/titleTextView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_centerHorizontal="true"
                    android:text="Authentication"
                    android:textSize="24sp"
                    android:textStyle="bold" />

                <!-- Subtitle -->
                <TextView
                    android:id="@+id/subtitleTextView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@id/titleTextView"
                    android:layout_centerHorizontal="true"
                    android:layout_marginTop="8dp"
                    android:text="Enter your details to authenticate"
                    android:textSize="16sp"
                    android:textColor="#808080" />

                <!-- Phone Number Prefix -->
                <TextView
                    android:id="@+id/phoneNumberPrefixTextView"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="+"
                    android:textSize="18sp"
                    android:layout_below="@id/subtitleTextView"
                    android:layout_marginTop="16dp"
                    android:layout_marginEnd="4dp"
                    app:layout_constraintTop_toBottomOf="@id/subtitleTextView"
                    app:layout_constraintEnd_toStartOf="@id/phoneNumberEditText"/>

                <!-- Customized TextBox for Phone Number -->
                <EditText
                    android:id="@+id/phoneNumberEditText"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:hint="Enter Phone Number"
                    android:inputType="phone"
                    android:layout_marginTop="16dp"
                    android:layout_below="@id/subtitleTextView"
                    android:layout_marginStart="4dp"
                    app:layout_constraintTop_toBottomOf="@id/subtitleTextView"
                    app:layout_constraintStart_toEndOf="@id/phoneNumberPrefixTextView"
                    app:layout_constraintEnd_toEndOf="parent"/>

                <!-- Authentication Button -->
                <Button
                    android:id="@+id/authenticationButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Authenticate"
                    android:layout_below="@id/phoneNumberEditText"
                    android:layout_marginTop="16dp"
                    app:layout_constraintTop_toBottomOf="@id/phoneNumberEditText"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"/>

                <!-- Authentication Code TextBox -->
                <EditText
                    android:id="@+id/authenticationCodeEditText"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:hint="Enter Authentication Code"
                    android:inputType="number"
                    android:layout_below="@id/authenticationButton"
                    android:layout_marginTop="16dp"
                    app:layout_constraintTop_toBottomOf="@id/authenticationButton"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"/>

                <!-- Validate Authentication Code Button -->
                <Button
                    android:id="@+id/validateButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Validate"
                    android:layout_below="@id/authenticationCodeEditText"
                    android:layout_marginTop="16dp"
                    app:layout_constraintTop_toBottomOf="@id/authenticationCodeEditText"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"/>

            </RelativeLayout>
        </androidx.cardview.widget.CardView>
    </ScrollView>

</RelativeLayout>
